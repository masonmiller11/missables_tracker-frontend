export const endpoints = {
    authentication : 'login',
    signup : 'signup',
    // listLeads: (itemsPerPage: number, page: number) => {
    //     return 'leads/' + page + '/' + itemsPerPage;
    // },
}